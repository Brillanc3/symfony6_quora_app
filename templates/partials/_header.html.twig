<header class="d-flex flex-row-nowrap justify-content-center p-20">
  <div class="content flex-fill d-flex flex-row-nowrap justify-content-between align-items-center">
    <a class="header-logo" href="{{ path('home') }}"><img src="{{ asset('images/logo.png') }}" alt="logo" /></a>
    <div class="header-search p-10 flex-fill">
      <i class="fas fa-search"></i>
      <input type="text" name="" id="" />
    </div>
    <div class="">
      {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
        <a href="{{ path('app_logout') }}" class="btn btn-primary">Déconnexion</a>
      {% else %}
        <a href="{{ path('app_register') }}" class="btn btn-primary">Inscription</a>
        <a href="{{ path('app_login') }}" class="btn btn-primary-stroke">Connexion</a>
      {% endif %}
    </div>
  </div>
</header>
